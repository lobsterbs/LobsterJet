name: zeolite

on:
  push:
  pull_request:
  schedule:
    - cron: "0 3 * * *" # nightly compat suite


jobs:
  rust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - name: diag fmt
        run: |
          cargo fmt --all --check > fmt.out 2>&1 || true
          if [ -s fmt.out ]; then
            D=$(head -c 700 fmt.out | sed ':a;N;$!ba;s/\n/%0A/g')
            echo "::error::FMT NOT CLEAN%0A$D"
          fi
      - name: diag clippy
        run: |
          cargo clippy --workspace --all-targets -- -D warnings > clippy.out 2>&1 || true
          if grep -q "^error" clippy.out; then
            D=$(grep -m1 -A8 "^error" clippy.out | head -c 700 | sed ':a;N;$!ba;s/\n/%0A/g')
            echo "::error::CLIPPY%0A$D"
          fi
      - name: diag test
        run: |
          cargo test --workspace > test.out 2>&1 || true
          if grep -qE "^error|FAILED|panicked" test.out; then
            D=$(grep -m1 -B2 -A8 -E "^error|FAILED|panicked" test.out | head -c 700 | sed ':a;N;$!ba;s/\n/%0A/g')
            echo "::error::TEST%0A$D"
          fi
      - name: diag wasm-check
        run: |
          cargo check -p zeolite-rewriter -p zeolite-wisp --target wasm32-unknown-unknown > wc.out 2>&1 || true
          if grep -q "^error" wc.out; then
            D=$(grep -m1 -A8 "^error" wc.out | head -c 700 | sed ':a;N;$!ba;s/\n/%0A/g')
            echo "::error::WASMCHECK%0A$D"
          fi

  wasm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: wasm32-unknown-unknown
      - run: cargo install wasm-pack --version 0.13.1 || true
      - name: Build rewriter wasm
        run: |
          if ! wasm-pack build crates/rewriter --target web --release --out-name rewriter_wasm > wasm1.out 2>&1; then
            D=$(grep -m1 -A10 -i "error" wasm1.out | head -c 700 | sed ':a;N;$!ba;s/\n/%0A/g')
            echo "::error::REWASM%0A$D"
            exit 1
          fi
          mkdir -p app/src/rewriter_wasm
          cp crates/rewriter/pkg/rewriter_wasm* app/src/rewriter_wasm/
      - name: Build wisp wasm
        run: |
          wasm-pack build crates/wisp-wasm --target web --release --out-name wisp_wasm
          mkdir -p app/src/wisp_wasm
          cp crates/wisp-wasm/pkg/wisp_wasm* app/src/wisp_wasm/
      - name: Measure wasm bundles (record; budget enforcement is the Phase 1 gate)
        run: du -b app/src/rewriter_wasm/rewriter_wasm_bg.wasm app/src/wisp_wasm/wisp_wasm_bg.wasm
      - uses: actions/upload-artifact@v4
        with:
          name: zeolite-wasm
          path: app/src/

  app:
    runs-on: ubuntu-latest
    needs: wasm
    steps:
      - uses: actions/checkout@v4
      - uses: actions/download-artifact@v4
        with:
          name: zeolite-wasm
          path: app/src/
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      # Vendor the libcurl transport (TLS terminates client-side in
      # libcurl.js; the SW cannot do TLS). The package is AGPL-3.0-only
      # with a ~2.1 MB bundled dist, so it is never committed: CI pulls a
      # pinned version and copies it into app/public/libcurl, where the
      # committed adapter (src/libcurl-transport-vendored.ts) loads it
      # at runtime. Deployments serving the built engine inherit AGPL
      # obligations for that bundle and its combined work.
      - name: Vendor libcurl transport
        run: |
          npm install --prefix .vendor @mercuryworkshop/libcurl-transport@2.0.5
          mkdir -p app/public/libcurl
          cp -r .vendor/node_modules/@mercuryworkshop/libcurl-transport/dist/. app/public/libcurl/
          # libcurl.js wasm binary, loaded by the transport at runtime.
          cp -r .vendor/node_modules/libcurl.js/dist app/public/libcurl/libcurl.js
          ls -la app/public/libcurl
      - run: cd app && npm install && npx tsc --noEmit && npm run build
      - name: Verify vendored transport made it into the build
        run: test -f app/dist/libcurl/index.mjs
      - name: Bootstrap size budget (must stay under 5 KB minified)
        run: |
          SIZE=$(wc -c < app/dist/bootstrap.js)
          echo "bootstrap.js: $SIZE bytes"
          test "$SIZE" -lt 5120
      - uses: actions/upload-artifact@v4
        with:
          name: zeolite-app
          path: app/dist/

  compat:
    # Nightly only: needs a built engine. Phase 1 gate: YouTube + Reddit
    # pass with first-paint ratio <= 2x.
    if: github.event_name == "schedule"
    runs-on: ubuntu-latest
    needs: [rust, app]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Build server + app, run probes locally
        run: |
          cargo build -p zeolite-server --release
          cd app && npm install && npm run build && cd ..
          ./target/release/zeolite-server --port 6002 &
          sleep 2
          node suite/probe.mjs --base http://localhost:6002
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lj-scoreboard
          path: suite/scoreboard.*
      # Publish the scoreboard as a public page (Phase 4): the latest
      # JSON + Markdown land on the "scoreboard" branch, force-updated
      # so the branch always reflects the newest nightly run.
      - name: Publish scoreboard branch
        if: always()
        run: |
          if [ ! -f suite/scoreboard.json ]; then
            echo "no scoreboard produced; skipping publish"
            exit 0
          fi
          git config user.name "zeolite-ci"
          git config user.email "ci@lobsterbrowse.invalid"
          git checkout --orphan scoreboard
          git rm -rf --quiet . || true
          cp suite/scoreboard.json suite/scoreboard.md . 2>/dev/null || cp suite/scoreboard.json .
          git add scoreboard.*
          git commit -m "scoreboard: nightly compat run ${GITHUB_RUN_ID}"
          git push -f origin HEAD:scoreboard
